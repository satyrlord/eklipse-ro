{
  "name": "eklipse-ro",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.mjs",
    "test:browser": "vite build --outDir tmp/browser-dist && playwright test",
    "ci": "npm test && npm run test:browser"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "parse5": "^8.0.0",
    "vite": "^7.1.1"
  },
  "allowScripts": {
    "esbuild@0.28.1": true
  }
}
