interface:
  display_name: "Run Quality Gate"
  short_description: "Run the static-site quality gate"
  default_prompt: "Use $run-quality-gate to run the eklipse static-site quality gate and report evidence."

policy:
  allow_implicit_invocation: false
